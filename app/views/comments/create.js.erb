$('#flash-messages-container').html("<%=j render partial: 'partials/flash_messages' %>")

  <% if @comment.persisted? %>
    $('#comments-form-container').html("<%=j render partial: 'comments/comment_form', locals: { comment: @new_comment, post: @post } %>")
  <% end %>
